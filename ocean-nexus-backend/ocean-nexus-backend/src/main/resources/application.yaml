server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ocean_nexus
    username: postgres
    password: Ridhinya@2008
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

python:
  fastapi:
    url: http://localhost:8000